function display_icon_trailer(topMargin, leftMargin, trailerConnected)
    if trailerConnected == true then
        trailerColor = COLOR_lightGreen
        Icon_Trailer(topMargin, leftMargin, trailerColor)
    end
end